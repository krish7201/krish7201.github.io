Got started:

installed node.js from pre-compiled binaries for windows
initialized npm in folder of project using cmd and command (npm init --yes)
installed live server using (npm install -g live-server)

launch using the .bat file